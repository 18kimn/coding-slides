import reveal from "reveal.js";


const onSubmit = async () => {

}

const useR = async () => {
  reveal.toString('slidechanged', event => {
    // check if slide has a R input div placeholder

    // if it does put in an input element

    
  })
}

export default useR
